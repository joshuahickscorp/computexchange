# Teardown · Control Room console unification · 2026-07-01

T0 for the CX Handoff · Control Room shift. Verified against the real code before
anything changes. Separator is `·` throughout (no em/en dashes). "Assumed" = what the
handoff spec stated; "Found" = what the codebase actually is.

## Goal recap

Rebuild `web/dashboard.html` in the demo's material language, absorb the admin views
from `web/skeleton.html`, delete skeleton, remove the `/app` route. `web/demo.html` is
untouched. One self-contained HTML file, no build step. Circular-light knob assets are
MIGRATED from `demo.html`, not regenerated.

## Routes + what serves what (control/api.go)

- `GET /{$}` → `handleDashboard` · serves `web/dashboard.html` · override `DASHBOARD_PATH` · api.go:1834
- `GET /app` → `handleApp` · serves `web/skeleton.html` · override `APP_PATH` · api.go:1868  ← TO REMOVE
- `GET /admin` + `/admin/{$}` → `handleAdminPage` · serves `web/admin.html` (the passkey panel built this session) · unrelated to this shift
- Admin data routes (bare-array JSON, authAdmin): `/admin/workers` `/admin/jobs` `/admin/payouts` `/admin/fraud-flags` `/admin/drift` `/admin/scheduler/explain`

## Admin read-endpoint shapes (verified · all bare arrays)

- `/admin/workers` → `[{id, supplier_id, hw_class, memory_gb, effective_memory_gb, throttled, avg_available_gb, memory_samples, last_seen_at, version, reputation, tier, status}]` · store.go ListWorkers
- `/admin/jobs` → `[{id, buyer_id, status, job_type, model_ref, tier, task_count, tasks_done, estimated_usd, actual_usd, created_at}]` · store.go ListJobsAdmin
- `/admin/payouts` → `[{supplier_id, payout_status, count, amount_usd}]` · store.go ListPayoutsAdmin
- `/admin/fraud-flags` → `[{supplier_id, reputation, tier, status}]` · store.go ListFraudFlags

## The monument · OWED · viable, with one correction

Per-row `amount_usd` exists on `/admin/payouts` ✓, so summing unreleased liability is
possible from a single authenticated fetch.

- **Assumed:** payout statuses are `held / ready / released`; monument = held + ready.
- **Found:** the real status set is `pending / held / released / clawed_back`
  (control/payment.go). There is NO `ready`. Money owed but not yet paid = `pending`
  and `held`. `released` = paid out; `clawed_back` = reversed fraud.
- **Decision:** the OWED monument sums `amount_usd` where `payout_status ∈ {held, pending}`,
  excluding `released` and `clawed_back`. Subline reads `held + pending · released and
  clawed-back excluded`. Rendered only from a successful authenticated fetch; absent an
  admin key or on any error the slot renders nothing (not a dash, not zero).

## Metrics the facts band reads (control/metrics.go · all six confirmed present)

- `cx_jobs_submitted_total` (counter) ✓
- `cx_tasks_completed_total` (counter) ✓
- `cx_queue_depth{tier,job_type}` (labeled gauge · MUST be summed across label series) ✓
- `cx_quarantines_total` (counter) ✓
- `cx_active_workers` (gauge) ✓
- `cx_verification_mismatch_total` (counter) ✓

Full cx_* surface (so cards match reality, nothing invented): also
`cx_tasks_dispatched_total`, `cx_payouts_released_total`, `cx_hedges_total`,
`cx_tiebreaks_total`, `cx_result_merges_total`, `cx_task_failures_total`,
`cx_quotes_total`, `cx_budget_stops_total`, `cx_long_poll_timeouts_total`,
`cx_ticker_seconds_since_success{ticker}` (labeled). A metric the exporter did not emit
renders `n/a` in the mute voice, never a fabricated 0.

## Buyer API shapes (for the submit panel)

- `POST /v1/jobs` body: `{job_type:{type}, model:{kind,ref}, tier, params, constraints:{min_memory_gb,...}, verification:{redundancy_frac,honeypot_frac,payout_hold_secs}, input (bare JSONL string OR {s3_key}), webhook_url (https only), max_usd?, quote_id?, min_reputation?, private_pool?}` → 202 `{job_id, task_count, estimated_usd, eta_secs, estimated_completion}`
- `GET /v1/jobs/{id}` → `{job_id, status, job_type, tier, task_count, tasks_done, estimated_usd, actual_usd, eta_secs, created_at, max_usd, budget_state, charge_status, verification}`
- `GET /v1/jobs/{id}/results` → `{job_id, status, results_url?, result_urls[]}`

### Job-type set · Assumed 6, Found 10

- **Assumed:** `embed, batch_infer, batch_classification, json_extraction, rerank, audio_transcribe`.
- **Found (types.go, validated api.go):** the six above PLUS `image_gen, eval, lora_finetune, custom`. The console select should offer all ten (or the shipped subset), not the skeleton's six.
- **Tiers (Found):** `batch, priority, trusted` (default `batch`). Matches assumed.

## Design source · what to migrate from web/demo.html (not regenerate)

- Fonts: Google Fonts link · `Cormorant+Garamond:wght@600` + `Geist+Mono:wght@400;500;600` (demo.html:13-15). If the console is served by the control plane it loads them the same way; a disk-open fallback drops to `ui-monospace`/`serif`.
- `:root` tokens: lift verbatim (bg #060606, panel #101012 / panel-hi #16161a, hair rgba(255,255,255,.05), ash/mute/bone/bone-hi/signal voices, green/red/gold anodized cores, mono/serif, radii 14/10/999, dur .18/.32 ease).
- **The circular light knob** (demo.html:110-122): `.knob` 236px, layered PNGs
  `assets/knob-off@3x.png` (base) · `knob-red@3x.png` (offline steady) · `knob-on@3x.png`
  (online, `knob-breathe` 4s) · `knob-pressed@3x.png` · `.knob-shadow` radial. Assets
  already live in `web/assets/`.
- **The sphere-from-within status dot** (demo.html:126-130, 193-209, 304-306): `assets/dot-ring@3x.png`
  ring + `radial-gradient(circle at 50% 40%, <core>, <hue> 72-74%)` `::before` + soft glow;
  `run-pulse` 2s for running; green/red/gold variants. Used for `.pip` (connection),
  `.run .dot`, `.stage .dot`, `.vmark`.
- Materials: `.pill` / `.pill.solid` buttons (demo.html:166-169), `.qs-snip` recessed
  snippet channel (253+), `.qs-toggle` quiet toggle (266). Flat panels, hairline
  structure only, no borders/chips/shadows, no blue.

## What breaks on demolition (must fix in the same shift)

- `scripts/prove-local.sh:979-982` · a live check curls `/app` and asserts the response
  contains `Supplier|Buyer|Workflows|skeleton`, recording `app-skeleton`. Removing `/app`
  WILL fail prove-local. Fix: repoint the check at `/` (the console) asserting a stable
  console string, or drop the `app-skeleton` ledger row. No `*_test.go` covers `/app`.
- Doc references to the `/app` skeleton (non-blocking, update for honesty): `README.md`,
  `RELEASE_CANDIDATE.md`, `docs/ALPHA_READINESS.md`, `docs/PRODUCT_SHAPE.md`.
- Supplier-tab `status.json` prose + the Workflows-via-jobs note relocate to
  `docs/SUPPLIER-SURFACE.md` (new) before the file is deleted.

## Skeleton content to port (behavior identical, restyled)

From `web/skeleton.html`: shared connection (base URL, buyer key, admin key, save) ·
buyer submit (model, job-type select, JSONL, optional webhook, submit) · buyer jobs
table + client-side id memory + results viewer · admin workers (throttle + effective
memory) · admin jobs · admin payouts · admin fraud-flags (snippet) · the stuck-jobs
runbook note. Every fetch error already surfaces verbatim; keep that total.

## Open design decisions (surface to owner before the rebuild)

1. **Circular-light knob placement.** Proposed migration: the knob becomes the console's
   connection/power hero in the header · `is-on` (green breathing) when the control plane
   is reachable, red steady when unreachable · semantically identical to its Earn role
   (power/connection). Confirm this vs a smaller placement.
2. **Relationship to the passkey `/admin` panel built this session.** `web/admin.html`
   (passkey-gated, at `/admin`) already renders workers/jobs/payouts/fraud/metrics. This
   shift rebuilds `web/dashboard.html` at `/` with an admin-KEY field per the handoff.
   That is two operator surfaces. Options: (a) ship the handoff as written and keep the
   passkey panel back-pocketed, (b) fold the Control Room design into the passkey `/admin`
   panel so there is one gated console. The handoff predates the passkey work; owner call.

## Gates for the shift

`make prove-local` green (after fixing the `/app` check) · no U+2014/U+2013 in any touched
file · `git grep -n skeleton` and `git grep -n '"/app"'` return nothing live · all
server strings through `esc()` · headless screenshots (disconnected, connected, admin) ·
`rg -n "5b9dff|border:1px solid|#171a21|#2a2f3a" web/dashboard.html` returns nothing.
