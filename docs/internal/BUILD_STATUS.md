# feat/perf-wave — Build Status Ledger

> Generated by the cx-perf-production-wave build. Branch is GREEN (cargo test 88/0/13-ignored; go build/test/vet clean). Honest status per item below.

# BUILD_STATUS — feat/perf-wave

## Pass 1 additions (post Candle expansion research)

- **build_hash content-id hardening — PROVEN.** `hardware::engine_build_hash` folds
  `infer_content_id()` (SHA-256 of `quantized_llama_batched.rs`). New unit test asserts a
  content-id change moves the class. Closes CANDLE_EXPANSION_RESEARCH L17. (agent: cargo test 89/0/13-ignored)
- **Qwen2 architecture-aware load — IMPLEMENTED; Qwen parity UNPROVEN.** `from_gguf` reads
  `general.architecture`, prefixes metadata keys with it (P-arch), dispatches NEOX vs interleaved
  rotary (P-rope), and loads optional q/k/v biases (P-qkvbias). Corrects the research's rope-only
  diagnosis: the official Qwen GGUFs are qwen2-arch and would not load on the old llama-only path.
  Llama byte-identical (network-free tests green); Qwen output parity needs a real GGUF + Metal.
  **Reseed golden hashes + honeypots** for the new `(device, candle, build_hash)` class.

## Activation pass (proof, run 2026-06-30 on an Apple M3 Pro, macOS 26.6, candle 0.10.2 + Metal)

This box's class: `device_label=metal, engine=candle, agent_version=0.1.0,
infer_content_id=c9a43b499231c46b, build_hash=29788fb25f948522` (classKey `candle|29788fb25f948522`).
Print it on any box with: `cargo test -p cx-agent print_current_verification_class -- --nocapture`.

- **Golden-hash gate — SEEDED + PROVEN (for this class).** `agent/tests/golden/llama32_1b_q4k_greedy.hashes`
  now holds 4 real rows (p001-p004) recorded via `CX_GOLDEN_RECORD=1 cargo test -p cx-agent
  golden_token_baseline_gate -- --ignored --test-threads=1 --nocapture`. A no-record re-run GATES green
  (p001-p004 OK). Record-time outputs were coherent (p002 -> "The capital of France is Paris.", p004 ->
  "2 + 2 = 4"). The Llama default is byte-identical across this pass, so the same hash values re-verified
  green after the `rope_dim` edit moved build_hash `cb257fe8535ef6c7` -> `29788fb25f948522`.
- **Qwen2.5-0.5B — PROVEN COHERENT + cross-checked (not byte-parity).** `qwen_05b_loads_and_is_coherent`
  (`#[ignore]`) loads the official qwen2-arch GGUF through the patched path and asserts coherent factual
  output: "The capital of France is ... Paris", "The largest planet ... Jupiter". llama.cpp greedy temp=0
  on the SAME GGUF for the same prompt also returns "Paris". Cross-engine BYTE parity is impossible by
  design, so this is a content/coherence proof, which a wrong NEOX rope or dropped q/k/v bias would fail.
  Run: `cargo test -p cx-agent qwen_05b_loads_and_is_coherent -- --ignored --test-threads=1 --nocapture`.
  - **Activation found one more gap and fixed it:** the Qwen GGUF omits `qwen2.rope.dimension_count`, so
    `from_gguf` now falls back to `head_dim` when the key is absent (Llama unaffected, key present). Without
    this the Qwen load errored ("cannot find qwen2.rope.dimension_count in metadata").
  - Cosmetic follow-up (NOT a correctness bug): with a raw (non-chat-templated) prompt the Qwen decode shows
    chat-control-token artifacts; the factual content is correct. Use a chat template for clean output.
- **Wave-1 perf-wave ignored tests — ALL PASS on Metal (run `--test-threads=1`).** `batch_shared_prefix_equals_serial`
  (shared-prefix == serial == bucketed), `batch_active_shrink_equals_serial_mixed_lengths` (batched == serial),
  `embed_bucketed_matches_single_pad` (bucketed == single-pad within 1e-4), `batched_vs_serial_throughput`
  (batched == serial for all 32; 124.9 vs 54.3 tok/s = ~2.3x even in a DEBUG build; release/A100 numbers still
  pending). CAVEAT: these Metal tests FAIL under the default parallel test runner because they share the one
  global Metal device; they are deterministic and pass with `--test-threads=1`. Production serializes per-model
  via the pool mutex, so this is a test-harness artifact, not a runtime issue.
- **Class-aware honeypots — NOT SEEDED (mechanism built; data is a placeholder).** The `honeypots.answer_class`
  column + class-pinned byte comparison exist (`db/schema.sql:213-223`, `control/verification.go` classKey), but
  the seeded batch_infer honeypot known_answer is the hand-written placeholder `{"text":"42"}`
  (`control/seed.go`), class-blind (`answer_class=''`, the safe default that never auto-quarantines). It cannot
  be marked class-aware as-is (it is not a real model output). EXACT operational reseed (do NOT fabricate the
  answer):
  1. On a worker of the target class (e.g. this M3 Pro's `candle|29788fb25f948522`), run the honeypot's
     batch_infer input through the real runner and capture the committed result BYTES.
  2. `UPDATE honeypots SET known_answer = '<those exact bytes>', answer_class = 'candle|29788fb25f948522'
     WHERE job_type='batch_infer' AND input_ref='honeypots/batch_infer/0001/input.jsonl';`
  3. Repeat per shipped class; embed/classification/json/rerank honeypots are tolerant-compared and may stay
     class-blind. Never set `answer_class` without a real same-class `known_answer`, or honest workers byte-fail.

Verified state of the perf-wave branch. Status is brutally honest: PROVEN means built AND a test or parity gate passes in this environment; IMPLEMENTED means it builds and is wired but needs hardware (GPU, a real GGUF, live Postgres, or two physical Macs) to prove; SCAFFOLDED means an honest compiling seam plus a written plan, inert by default; DEFERRED means not started.

Branch build at time of writing: agent `cargo build` GREEN, `cargo test --release` 88 passed / 0 failed / 13 ignored; control `go build ./...` GREEN, `go test ./...` ok, `go vet ./...` clean. Nothing committed, pushed, or deployed.

## Release-mode Metal re-run (2026-07-01, same M3 Pro, `cargo test --release`)

All 13 previously-`#[ignore]`'d tests + `pool_loads_real_model_once` (14 total) re-run in a
RELEASE build with `--test-threads=1`, 15GB of models already HF-cached: **14 passed / 0
failed**. `golden_token_baseline_gate` re-verified p001-p004 green against the existing
`candle|29788fb25f948522` class (no re-seed needed — build_hash unchanged since Pass 1).
`qwen_05b_loads_and_is_coherent` reconfirmed factually coherent ("Paris", "Jupiter").

**batched_vs_serial_throughput, release build: 1536 tok in 9.10s batched vs 13.84s serial =
168.8 vs 111.0 tok/s = 1.5x, correctness OK (batched == serial for all 32).** This closes
the "release/A100 numbers still pending" note above for the Metal half — worth flagging
honestly: 1.5x release is LOWER than the 2.3x recorded in a DEBUG build in Pass 1 (same
test, same 32-prompt/48-tok fixture). Reads as the release compiler optimizing the serial
baseline harder than the batched path narrows the gap it needs to close, not a regression
in the batching logic itself (correctness held in both runs) — worth a look before citing a
speedup number externally, but not a blocker. The A100/CUDA release-mode number is still
open; per docs/VLLM_LANE.md that needs the vLLM determinism soak infra, not a bare rerun.

## Wave 0: fork surface, engine-tag axis, determinism class

| Item | Status | Files | Determinism note | Exact next action |
|---|---|---|---|---|
| Candle patch surface (vendored-module strategy, P1-P5 ledger) | PROVEN | docs/CANDLE_FORK.md | No code change to byte output; documents the patch surface only. | None. Re-run the upstream-sync procedure in the doc when bumping Candle 0.10.x. |
| KV-cache preallocation via slice_set (replaces per-step Tensor::cat) | PROVEN | agent/src/quantized_llama_batched.rs | SAFE. slice_set writes identical bytes at the same (batch, head, position, dim) coords cat wrote; logits bitwise-identical. Pinned by prealloc_kv_append_matches_cat plus reset/compact/fork tests. | Run the GPU-ignored batched-vs-serial parity tests on a real GGUF to confirm on Metal/CUDA. |
| (device, engine, build_hash) verification class end to end | PROVEN | agent/src/hardware.rs, types.rs; control/types.go, api.go, store.go, benchmark.go, scheduler.go, verification.go, control_test.go; db/schema.sql; proto/manifest.schema.json | HARDENING, changes no byte output. Byte-exact compare/quarantine only WITHIN a matched class; cross-class diff is provisional trust, never an auto-dock. Empty build_hash is unknown and never byte-compared. Pinned by TestSameVerificationClass, TestClassKey, TestMatchPinsVerificationClass, TestMatchSameClassOnly, TestMatchHWClassConstraint, plus live prove-local redundancy/honeypot. | Surface redundancy_cross_class / tiebreak_cross_class counts in the operator view (recorded, not yet shown). |
| Golden-hash regression gate (Hawking transplant) | IMPLEMENTED | agent/src/runners.rs, agent/tests/golden/llama32_1b_q4k_greedy.hashes | SAFE. Records/asserts greedy token-baseline hashes WITHIN one class; the .hashes file is confirmed UNSEEDED (zero data rows, comments only) so the guard is currently inert. | Run runners::tests::golden_token_baseline_gate with CX_GOLDEN_RECORD=1 on each reference (device, engine, build_hash) box (needs the ~800MB GGUF), then commit the recorded rows. |
| engine-tag axis (InferenceBackend::engine_tag) | PROVEN | agent/src/config.rs | SAFE. Identity tag only, no output change. Pinned by engine_build_hash_is_stable_and_sensitive. | None. |

## Wave 1: in-house throughput

| Item | Status | Files | Determinism note | Exact next action |
|---|---|---|---|---|
| Prefix-KV sharing for classification/extraction (generate_batch_shared_prefix) | PROVEN (in-process); live parity IMPLEMENTED | agent/src/runners.rs, agent/src/quantized_llama_batched.rs | SAFE and byte-exact. Shared prefix is the longest common TOKEN prefix (computed after full tokenization, no merge-boundary hazard); restore re-seats bitwise-identical KV; remainder prefilled at index_pos=prefix_len. Token-for-token identical to serial generate. Pinned by prefix_shared_prefill_matches_inline, prefix_shared_falls_back_when_no_useful_prefix, snapshot_restore_continues_prefix_verbatim. | Run batch_shared_prefix_equals_serial on a GPU box (it is #[ignore]); then measure speedup on high-prefix-ratio batches. |
| Length-bucketed embedder/reranker (Embedder::embed plus embed_bucket) | PROVEN (in-process); live parity IMPLEMENTED | agent/src/runners.rs | SAFE. Buckets keyed by exact token length, members in stable input order, scatter-back is an unambiguous permutation. rerankAgree needs exact order equality and meanCosine pairs by position, both preserved. Pinned by embed_bucketing_scatters_back_to_original_order. | Run embed_bucketed_matches_single_pad on a GPU box (it is #[ignore]); confirm cosine > 0.9999 and identical order on the real BERT. |

## Wave 2: bigger bets

| Item | Status | Files | Determinism note | Exact next action |
|---|---|---|---|---|
| Throughput-aware claim tiebreak (worker_tps then bw_gbps in ClaimTask) plus benchmark index | PROVEN (build/test/vet); live assertion DEFERRED | control/scheduler.go, db/schema.sql | SAFE. Claim SELECTION only; re-orders tasks a worker is already eligible for, touches no inference path or verification.go. Matches the audit ledger "schedule on bw_gbps tiebreak, selection only, SAFE". | Integration-test under live Postgres: two same-class workers at different tps for two equal-age job types, assert each claims its faster type first. |
| vLLM CUDA serving-lane seam (InferenceBackend::Vllm, VllmRunner) | SCAFFOLDED | agent/src/config.rs, main.rs, runners.rs; docs/VLLM_LANE.md | Inert by default. run() returns NotImplemented when CX_VLLM_BASE_URL is unset AND, by design, also when set (confirmed: `let _ = base_url;` then a second NotImplemented) so it never emits unverified bytes. When wired, output is only ever byte-compared within nvidia_* plus engine=vllm. Pinned by vllm_runner_gates_llm_jobs_and_surfaces_boundary. | Run the docs/VLLM_LANE.md de-risk spike (two pinned nvidia_* workers, cross-SKU plus restart byte-stability soak); only then wire run() shell-out via sandbox::run_sandboxed. |
| Hawking continuous-batch lane skeleton (InferenceBackend::Hawking, continuous_batch module) | SCAFFOLDED | agent/src/continuous_batch.rs, main.rs, config.rs; docs/HAWKING_PORT_PLAN.md | Inert. Pure, GPU-free, wired to no kernel; engine=hawking falls back to generate_batch, so behavior is unchanged today. group_by_prefix provably deterministic. 7 unit tests pass; #![allow(dead_code)] because the decode loop is not yet wired. | Port the Metal multi-seq slot-strided KV kernel (forward_multiseq_*), add a HawkingRunner consuming decode_plan, then run B=8 batched==serial atol=1e-3 parity plus the cross-Mac class-boundary test. |

## Wave 3: frontier / spike-first (all DEFERRED)

| Item | Status | Files | Determinism note | Exact next action |
|---|---|---|---|---|
| SDPA fast-path ungate on CUDA | DEFERRED | docs/CANDLE_FORK.md (documented only) | Changes CUDA byte-output; must NOT ship silently. Metal/CPU gate (is_metal() && seq_len==1) unchanged. | Implement behind the nvidia_* class with a within-tier byte-stability soak plus hw_class-aware honeypots, alongside the vLLM lane. |
| vLLM run() shell-out plus CUDA determinism soak | DEFERRED | docs/VLLM_LANE.md | The load-bearing remainder of the vLLM lane; gated entirely on the soak, not the FFI. | Stand up two pinned A100/H100 vLLM workers; prove byte-identical greedy across SKUs and restarts; encode SKU into build_hash if A100 != H100. |
| Hawking Metal kernel plus HawkingRunner (weeks 2-5) | DEFERRED | agent/src/continuous_batch.rs (skeleton only); docs/HAWKING_PORT_PLAN.md | Batched path is atol=1e-3, not bit-identical to solo; same cross-hw fp16 risk the status quo carries, re-gated within (apple, hawking, build_hash). | Port forward_multiseq_*, apply_decode_*/sample_next, PrefixIndex KV-copy; wire HawkingRunner; prove B=8 parity. |
| RWKV-7 SSM long-context lane | DEFERRED | none (Hawking source: shaders/rwkv7.metal, rwkv_decode_arena.rs) | needs_care: LOGIT_TOL=0.05; recurrent state cascades a single argmax flip irreversibly. TQ integer-deterministic path off by default. | Prereq the deferred instruct post-train plus a per-bit quality card; then port behind its own class. |
| Constrained/grammar (JSON-schema) decoding in the batch lane | DEFERRED | none (Hawking source: JsonConstraint/JsonVocabIndex) | Capability unlock, not throughput. | Port JsonConstraint into the batch lane; user grammars need crate-edge work. |
| F16 KV cache opt-in long-context lane | DEFERRED | none | 88 percent argmax-identity, NOT byte-identical; MUST be a separate non-byte-exact lane or it breaks generation verification. | Build as a distinct non-byte-exact lane with its own engine tag. |
| vLLM larger-model band (13B/32B/70B) | DEFERRED | none (catalogue: models.rs is_big_llama) | Capability; memory gate already exists. | Add the SKUs to the catalogue after the vLLM lane lands. |
| Ragged/batched remainder decode for generate_batch_shared_prefix | DEFERRED | agent/src/runners.rs (TODO note inline) | The full 2-4x; prefill sharing already token-identical. | expand_kv_cache(B) of the shared prefix then bucket the remainder by length like generate_batch. |
| hw_class-aware honeypot seeding | DEFERRED | control/seed.go (answer_class blank), db/schema.sql (column exists, honored) | SAFE default today: byte-exact honeypots do NOT auto-quarantine because answer_class is empty. | Record each known answer's producing class into honeypots.answer_class (audit de-risk #1 before vLLM go-live). |
| Per-model quant in build_hash | DEFERRED | agent/src/hardware.rs (CATALOGUE_QUANT fixed q4_k_m) | A requant should land in a distinct class automatically. | Derive CATALOGUE_QUANT from the loaded weights when a runtime varies quant per model. |

## Cross-cutting follow-ups (not tied to one wave)
- Two-physical-Mac field test: cross-worker within-class comparison is proven on one box (two agents); real two-Mac hardware is the only un-run validation of the class boundary.
- The 13 #[ignore] Rust tests are seed-then-gate or GPU-gated proofs, not breakage: golden_token_baseline_gate (unseeded), batch_active_shrink_equals_serial_mixed_lengths, batch_shared_prefix_equals_serial, embed_bucketed_matches_single_pad, batched_vs_serial_throughput. Run them on the reference Metal/CUDA box.

## Next actions
- Record the golden-hash baseline: run agent runners::tests::golden_token_baseline_gate with CX_GOLDEN_RECORD=1 on the reference box of each shipped (device, engine, build_hash) class (needs the ~800MB GGUF), then commit the rows into agent/tests/golden/llama32_1b_q4k_greedy.hashes so the within-class regression guard is no longer inert.
- Run the 13 GPU-ignored parity/throughput tests on a real Metal/CUDA box with the GGUF: batch_shared_prefix_equals_serial, embed_bucketed_matches_single_pad, batch_active_shrink_equals_serial_mixed_lengths, batched_vs_serial_throughput to confirm byte/token parity and measure the Wave 1 speedups.
- Seed hw_class-aware honeypots (control/seed.go answer_class is blank today, the safe default): record each known answer's producing class so byte-exact honeypots re-enable auto-quarantine WITHIN a class. This is the audit's named de-risk step #1 before the vLLM lane go-live.
- Run the vLLM CUDA determinism soak from docs/VLLM_LANE.md (two pinned nvidia_* workers, cross-SKU A100/H100 plus restart byte-stability) BEFORE wiring VllmRunner::run shell-out; encode the SKU into build_hash if A100 and H100 diverge under the pin.
- Integration-test the scheduler tiebreak under live Postgres (scripts/prove-local.sh fixture): two same-class workers benchmarked at different tps for two equal-age/tier job types, asserting each claims its faster type first.
- Have the orchestrator write the returned ledger_md to docs/BUILD_STATUS.md (this stage was write-the-ledger only and edited no code); the file does not exist yet.
