# cx-agent idle-footprint audit — first run

> Produced by `scripts/idle-audit.sh` (docs/internal/CREED_AND_PATH_TO_TEN.md, "Agent
> idle footprint & startup overhead" 5→6). This run is a SHORT verification window
> (60s), not the doc's own stated default (10 minutes) — it exists to prove the script
> works end-to-end against a real, registered `cx-agent run` process on real local
> infrastructure (ephemeral Postgres + MinIO + control plane, this session), not to be
> the definitive idle-budget record. Re-run with the default `DURATION_SECS=600` on a
> real supplier box for that. `idle wakeups/sec` is honestly omitted below — this
> session has no non-interactive `sudo`, and the script refuses to guess rather than
> fabricate a number it did not measure.

Generated 2026-07-05T18:25:37Z · pid 56643 (real `cx-agent run`, registered against a
real local control plane) · window 60s · 12 samples every 5s

| metric | measured | budget | verdict |
|---|---|---|---|
| avg %CPU | 0.000% | < 0.5% | within budget |
| max %CPU | 0.000% | (informational) | |
| avg RSS | 17.2 MB | < 80 MB | within budget |
| max RSS | 17.2 MB | (informational) | |

**idle wakeups/sec:** not measured this run — requires `sudo powermetrics` (root-only on macOS); see script output above.

**network (nettop, best-effort):** `14:25:32.304489,tcp4 127.0.0.1:57133<->127.0.0.1:8099,lo0,Established,1875,2757,0,0,0,1.00 ms,406656,405632,BE,-,cubic,-,-,-,-,so,`

**status.json:** last write to status.json: Sun Jul  5 18:25:15 UTC 2026 (mtime sampled once at report time, not tracked continuously)

Raw per-sample data: `.artifacts/idle-audit/raw-20260705T182431Z.tsv`
